# Wix Archive

This folder preserves the original Wix Git Integration export while Floraseek is converted into a standalone Vite and React website for Vercel.

The archived files are kept for reference only. The production React app does not import or depend on `$w`, Velo modules, Wix Studio element IDs, or `wix.config.json`.
